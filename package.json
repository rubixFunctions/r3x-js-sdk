{
  "name": "@rubixfunctions/r3x-js-sdk",
  "version": "0.0.14",
  "description": "SDK to allow for ease of development of a function as a container",
  "repository": {
    "type": "git",
    "url": "https://github.com/rubixFunctions/r3x-js-sdk"
  },
  "main": "dist/r3x.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "mocha ts-node/register tests/**/*.test.ts",
    "testWithCoverage": "nyc -r lcov -e .ts -x \"*.test.ts\" mocha -r ts-node/register tests/**/*.test.ts && nyc report",
    "check": "tsc",
    "clean": "tsc",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "start": "npm run compile && node ./hello-r3x.js"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.0",
    "chai": "^4.2.0",
    "gts": "^0.9.0",
    "mocha": "^6.0.2",
    "nyc": "^13.3.0",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.1"
  },
  "dependencies": {}
}
